import AppLayout from "@/components/AppLayout";

export default function StudentAdd() {
    return (
        <AppLayout>
            <h1 className="text-2xl font-bold">Student Add</h1>
            <p className="text-xs text-gray-500">Add a new student record to the system</p>
        </AppLayout>
    );
};